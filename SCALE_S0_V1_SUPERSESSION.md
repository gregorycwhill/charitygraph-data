# S0 V1 shadow supersession

`SCALE_S0_SHADOW_MANDATE.yaml` and its calibration/shadow-run chronology remain
historical retained evidence. They are non-executable and superseded as candidate
authority by the immutable V2 policy bundle and V2 shadow mandate. In particular,
the repeated `B814…D85E` value was not an individual-policy content hash, the
160-task arithmetic excluded eight human conduct tasks, and the former review
total omitted deterministic audits.
